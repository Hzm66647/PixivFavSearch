# PixivFavSearch 常见问题 FAQ

## 1. 启动后浏览器没自动打开？
手动打开浏览器，地址栏输入 `http://127.0.0.1:8897` 回车即可。如果也打不开，可能是服务没启动成功，看第 2 条。

## 2. 双击启动.bat 后窗口一闪就没了 / 8897 端口打不开？
- 检查是否被防火墙拦截。Windows 可能会弹出防火墙提示，点「允许」
- 检查 8897 端口是否被其他程序占用：打开命令行输入 `netstat -ano | findstr :8897`，如果有结果说明端口被占用了
- 如果是首次启动，需要联网下载便携 Python，请确保网络通畅

## 3. 搜索没结果 / 搜不到东西？
- 如果你还没导出自己的收藏，用的是示例数据 —— 示例数据只有少量作品，可以搜「初音」「fate」试试
- 如果你已经导出了自己的收藏，检查 `data/` 目录下是否有 `.json` 文件
- 检查搜索关键词是否拼写正确（日文作品名用罗马音也能搜，如 `sakura` 可搜到 `サクラ`）

## 4. 缩略图加载不出来？
- 缩略图需要从 Pixiv 服务器下载，国内访问 Pixiv 可能需要代理
- 如果提示「Pixiv 不可达」，请先开启你的代理软件（如 v2rayN、Clash），然后刷新页面
- 缩略图下载失败不影响搜索功能，只是没有图片预览

## 5. 导出收藏时提示「Pixiv 登录失败」？
- 确保你的网络可以访问 Pixiv（可能需要代理）
- 浏览器弹出 Pixiv 登录页后，请手动登录你的账号
- 登录成功后不要关闭页面，脚本会自动完成抓取
- 如果还是失败，请确认已用调试模式打开浏览器且已登录 Pixiv。

## 6. 如何更新到新版本？
- 下载新版 zip 包，解压覆盖旧文件即可
- **数据不会丢失**，你的收藏数据在 `data/` 目录下，覆盖时保留 `data/` 文件夹即可
- 另外，程序启动时会自动检查 GitHub 上是否有新版本，有更新会提示你

## 7. 安全吗？我的 Cookie 会泄露吗？
- **Cookie 不会保存在硬盘上**。导出收藏时，浏览器临时登录，脚本只在内存中使用 Cookie 完成抓取，不会写入文件
- 所有代码都在本地运行，没有数据上传到任何第三方服务器
- 如果你不放心，可以查看源码 `pix_export.py`，总共只有几百行，很容易验证

## 8. 我可以在其他设备上使用吗？
- 目前仅支持 Windows
- 可以在同一局域网的其他设备上访问：在服务端找到本机 IP（命令行输入 `ipconfig`），另一台设备的浏览器打开 `http://你的IP:8897`
- 注意：不要暴露到公网，否则你的收藏数据可能被他人访问

## 9. 收藏数据存在哪里？格式是什么？
- 数据存在 `data/` 目录下，JSON 格式
- 数据结构和 Pixiv API 返回的字段一致，包含作品 ID、标题、标签、作者、简介等
- 如果你懂技术，可以直接编辑 JSON 文件来增删改收藏

## 10. 如何完全卸载？
- 删除整个 PixivFavSearch 文件夹即可
- 不会在系统里留下任何残留（没有注册表、没有系统服务、没有环境变量）
- 如果你下载过便携 Python，也在 `python/` 文件夹里，一起删掉就干净了